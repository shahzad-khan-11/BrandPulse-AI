# ──────────────────────────────────────────────────────────
# BrandPulse AI — Root Environment Variables
# This file is only used when running with docker-compose.
# For individual service env vars, see:
#   backend/.env.example
#   frontend/.env.example
# ──────────────────────────────────────────────────────────

# Gemini AI (shared root-level for docker-compose override)
GEMINI_API_KEY=YOUR_GEMINI_API_KEY_HERE
